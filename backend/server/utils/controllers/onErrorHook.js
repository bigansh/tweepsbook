/**
 * A onError hook to add middlewares.
 *
 * @param {import('fastify').FastifyRequest} req
 * @param {import('fastify').FastifyReply} res
 */
const onError = async (req, res) => {}

module.exports = onError
